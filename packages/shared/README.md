# @ggez/shared

Shared types and utilities used across the Web Hammer package set.

This package contains common scene contracts, transforms, graph helpers, and other foundational utilities consumed by editor, runtime, and build packages.

## Install

```bash
bun add @ggez/shared
```